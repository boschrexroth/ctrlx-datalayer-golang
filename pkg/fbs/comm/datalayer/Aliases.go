// Code generated by the FlatBuffers compiler. DO NOT EDIT.

package datalayer

import (
	flatbuffers "github.com/google/flatbuffers/go"
)

type AliasesT struct {
	Groups []*AliasGroupT `json:"groups"`
}

func (t *AliasesT) Pack(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	if t == nil { return 0 }
	groupsOffset := flatbuffers.UOffsetT(0)
	if t.Groups != nil {
		groupsLength := len(t.Groups)
		groupsOffsets := make([]flatbuffers.UOffsetT, groupsLength)
		for j := 0; j < groupsLength; j++ {
			groupsOffsets[j] = t.Groups[j].Pack(builder)
		}
		AliasesStartGroupsVector(builder, groupsLength)
		for j := groupsLength - 1; j >= 0; j-- {
			builder.PrependUOffsetT(groupsOffsets[j])
		}
		groupsOffset = builder.EndVector(groupsLength)
	}
	AliasesStart(builder)
	AliasesAddGroups(builder, groupsOffset)
	return AliasesEnd(builder)
}

func (rcv *Aliases) UnPackTo(t *AliasesT) {
	groupsLength := rcv.GroupsLength()
	t.Groups = make([]*AliasGroupT, groupsLength)
	for j := 0; j < groupsLength; j++ {
		x := AliasGroup{}
		rcv.Groups(&x, j)
		t.Groups[j] = x.UnPack()
	}
}

func (rcv *Aliases) UnPack() *AliasesT {
	if rcv == nil { return nil }
	t := &AliasesT{}
	rcv.UnPackTo(t)
	return t
}

type Aliases struct {
	_tab flatbuffers.Table
}

func GetRootAsAliases(buf []byte, offset flatbuffers.UOffsetT) *Aliases {
	n := flatbuffers.GetUOffsetT(buf[offset:])
	x := &Aliases{}
	x.Init(buf, n+offset)
	return x
}

func GetSizePrefixedRootAsAliases(buf []byte, offset flatbuffers.UOffsetT) *Aliases {
	n := flatbuffers.GetUOffsetT(buf[offset+flatbuffers.SizeUint32:])
	x := &Aliases{}
	x.Init(buf, n+offset+flatbuffers.SizeUint32)
	return x
}

func (rcv *Aliases) Init(buf []byte, i flatbuffers.UOffsetT) {
	rcv._tab.Bytes = buf
	rcv._tab.Pos = i
}

func (rcv *Aliases) Table() flatbuffers.Table {
	return rcv._tab
}

func (rcv *Aliases) Groups(obj *AliasGroup, j int) bool {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		x := rcv._tab.Vector(o)
		x += flatbuffers.UOffsetT(j) * 4
		x = rcv._tab.Indirect(x)
		obj.Init(rcv._tab.Bytes, x)
		return true
	}
	return false
}

func (rcv *Aliases) GroupsByKey(obj *AliasGroup, key string) bool{
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		x := rcv._tab.Vector(o)
		return obj.LookupByKey(key, x, rcv._tab.Bytes)
	}
	return false
}

func (rcv *Aliases) GroupsLength() int {
	o := flatbuffers.UOffsetT(rcv._tab.Offset(4))
	if o != 0 {
		return rcv._tab.VectorLen(o)
	}
	return 0
}

func AliasesStart(builder *flatbuffers.Builder) {
	builder.StartObject(1)
}
func AliasesAddGroups(builder *flatbuffers.Builder, groups flatbuffers.UOffsetT) {
	builder.PrependUOffsetTSlot(0, flatbuffers.UOffsetT(groups), 0)
}
func AliasesStartGroupsVector(builder *flatbuffers.Builder, numElems int) flatbuffers.UOffsetT {
	return builder.StartVector(4, numElems, 4)
}
func AliasesEnd(builder *flatbuffers.Builder) flatbuffers.UOffsetT {
	return builder.EndObject()
}
